using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp4
{
    /*public class Player : IComparable
    {
        private string name;
        private int runs;
        public Player(string name,int runs)
        {
            this.name = name;
            this.runs = runs;
        }
        public int compareTo(object obj)//object of player4
        {
            Player p4 = (Player)obj;//type casting
            //to access runs of player1 we use this
            //this point to current object
            if (this.runs > p4.runs)
            {
                return 1;
            }
            else if (this.runs < p4.runs)
            {
                return -1;
            }
            else
            {
                return 0;
            }
        }
        public override string ToString()
        {
            return$"{name} {runs}";
        }

    }
    public class Team : IEnumerable
    {

    }*/
}
